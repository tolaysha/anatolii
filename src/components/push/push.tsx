"use client";

import React from "react";
import Button from "@mui/material/Button";
import styles from "./push.module.css";

export default function PUSH() {
  return (
    <Button className={styles.pushButton}>
      PUSH
    </Button>
  );
}